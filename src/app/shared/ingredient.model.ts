export class Ingredient {
    /*public name: string
    public amount: number*/

    //Shotcut for not having to declare the variables and assign values to them in the body of the constructor
    constructor(public name: string, public amount: number){}
}
